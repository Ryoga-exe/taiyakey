# taiyakey

たい焼きのキーボード、たいやキー

## Development

```sh
npm install
npm run setup:dict
npm run build:dict
npm run dev
```

The dictionary JSON files are generated from Python `wordfreq` Zipf
frequencies and are used only as static browser assets.
